ANTHROPIC_API_KEY=
FIRECRAWL_API_KEY=
POSTIZ_API_KEY=
POSTIZ_BASE_URL=https://app.postiz.com/api/public/v1
POSTIZ_TUNNEL_URL=  # Stable URL for named tunnel (e.g. https://postiz.mertokar.com). Falls back to auto-detection from Docker logs if unset.
NOTIFY_EMAIL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
BRAND=carephoto
CAREPHOTO_API_KEY=  # carephoto Agent API key (ck_live_...) — required when brand.imageGeneration.enabled
CAREPHOTO_BASE_URL=https://carephoto.art/api/v1/agent
